---
name: Bug report
about: Something in AutoFTE isn't working the way it should
title: "[Bug] "
labels: bug
assignees: ""
---

**Describe the bug**
A clear, concise description of what's wrong.

**Command run**
```
autofte ...
```

**Expected behavior**
What you expected to happen instead.

**Actual behavior**
What actually happened — paste the terminal output (`--verbose` if relevant), and any relevant contents of `crash_triage.json` / `binary_analysis.json` / `llm_analysis.json`.

**Environment**
- AutoFTE version: `autofte --version` (or `git rev-parse HEAD` if running from source)
- Install method: pipx / source checkout / Docker / conda
- OS + Python version: `python3 --version`
- Output of `autofte doctor`:
```
(paste here)
```

**Additional context**
Anything else that might help — a minimal reproducing binary/crash file, if you can share one.
